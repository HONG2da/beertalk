<!-- navbar begin -->
  <nav class="navbar navbar-toggleable-md navbar-light bg-faded">
    <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <a class="navbar-brand" href="#">SNS</a>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item active">
          <a class="nav-link" href="/post/write">자유게시판 <span class="sr-only">(current)</span></a>
        </li>
      </ul>
      <div class="my-auto" style="padding-right: 10px;">
        <% if user_signed_in? %>
          <%= current_user.name %>
          <%= link_to 'Edit', edit_user_registration_path, method: 'get' %>
          <%= link_to 'Sign Out', destroy_user_session_path, method: 'get' %>
        <% else %>
          <a href ="/users/sign_in" style="padding-right: 10px;">Sign In</a>
          <a href ="/users/sign_up">Sign Up</a>
        <% end %>
      </div>
      <form class="form-inline my-2 my-lg-0">
        <input class="form-control mr-sm-2" type="text" placeholder="Search">
        <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Search</button>
      </form>
    </div>
  </nav>
<!-- navbar end -->

<!-- container begin -->
  <div class="container" style="margin-top:50px;">

    <!-- write form begin -->
      <form action="/post/update/<%= @posts.id %>" method="POST" style="margin-bottom:15px; padding-top: 10px;padding-right: 10px;padding-bottom: 10px;padding-left: 10px; border:1px solid">
        <div class="form-group">
          <label for="exampleInputEmail1">제목</label>
          <input name="title" value="<%= @posts.title %>"type="text" class="form-control" id="exampleInputEmail1" aria-describedby="emailHelp" placeholder="제목을 입력하세요.">
        </div>
        <div class="form-group">
          <label for="exampleTextarea">내용</label>
          <textarea name="content" class="form-control" id="exampleTextarea" rows="5" placeholder="제목을 입력하세요."><%= @posts.content %></textarea>
        </div>
        <% if user_signed_in? %>
            <div style="text-align:right; margin-bottom:50px;">
              <button type="submit" class="btn btn-primary">작성하기</button>
            </div>
        <% end %>
      </form>
    <!-- write form end -->

  </div>
<!-- container end -->
