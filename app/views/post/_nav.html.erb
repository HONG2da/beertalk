<nav class="navbar navbar-toggleable-md navbar-light bg-faded" style="background-color: #f0ad4e;" >
  <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <a class="navbar-brand" href="/post/list">맥주 테이스팅 노트</a>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <a href="#">
          <span class="sr-only">(current)</span>
        </a>
      </li>
    </ul>
    <div class="my-auto" style="padding-right: 10px;">
        <% if user_signed_in? %>
            <%= link_to '로그아웃', destroy_user_session_path, method: 'get' %>
            <%= link_to '글 등록', post_write_path, method: 'get' %>
        <% else %>
            <a href="/users/sign_in" style="padding-right: 10px;">로그인</a>
            <a href="/users/sign_up" style="padding-right: 10px;">회원가입</a>
        <% end %>
        </div>
  </div>
</nav>